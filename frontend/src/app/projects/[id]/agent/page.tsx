"use client";

/** Agente Cowork en pantalla completa (zona Vex Cowork).
 *  La misma experiencia vive como panel lateral plegable en todo el
 *  proyecto — ver CoworkAgent (components) y el dock en el layout. */

import CoworkAgent from "@/components/CoworkAgent";

export default function CoworkAgentPage() {
  return <CoworkAgent />;
}
